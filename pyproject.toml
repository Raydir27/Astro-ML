[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "astro_ml"
version = "0.1.0"

[tool.setuptools]
# This tells Python that 'src' is the root of your code
packages = { find = { where = ["."] } }